# Skate Performance • Time Brasil V5.6

Correção do fundo branco do uploader:
- agora o projeto define o tema NATIVO do Streamlit em `.streamlit/config.toml`;
- `secondaryBackgroundColor` = #0B2034, que é a cor usada pelos widgets nativos, inclusive os cards dos arquivos carregados;
- sidebar e widgets usam fundo dark e texto claro;
- mantém integralmente a V5.5 nos cálculos, gráficos e PDFs.

IMPORTANTE: envie também a pasta oculta `.streamlit` para o GitHub.
Ela contém `config.toml` e é necessária para esta correção.
